module.exports = {
  presets: [
    'next/babel', // Use 'next/babel' preset for Next.js compatibility
    '@babel/preset-typescript', // Include TypeScript preset if needed
    // Add other presets as necessary
  ],
  // Add any other Babel configuration options here if needed
};
